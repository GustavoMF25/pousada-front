export default function BodyLogin(props: any) {
    return (
        <section className="g-gray-50 dark:bg-gray-900">
            {/* <section  className="flex flex-col items-center h-screen w-screen justify-center backdrop-blur from-green-600 to-green-950 bg-gradient-to-r"> */}
            {props.children}
        </section >
    )
}